# Book and Library Implementation Plan

## Steps:

1. [x] Update Book class: add title, author, is_borrowed flag, toggle_borrow() method.
2. [x] Rewrite Library class: init(name), self.books=[], add_book(title, author), remove_book(title), search_by_title(title), lend_book(title), return_book(title).
3. [x] Add example usage/demo at end of student_scores.py.
4. [x] Test functionality manually.
